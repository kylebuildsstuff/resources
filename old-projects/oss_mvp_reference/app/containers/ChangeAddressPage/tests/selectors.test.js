// import { selectChangeAddressDomain } from '../selectors';
// import { fromJS } from 'immutable';

// const selector = selectChangeAddressDomain();

describe('<selectChangeAddressDomain />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toBe(true);
  });
});
