import styled from 'styled-components';

export const BackgroundDropdownMenuStyles = styled.div`
  position: absolute;
  top: 0;
  left: 0;
  bottom: 0;
  right: 0;
  background: rgba(0, 0, 0, 0.15);
`;

export default BackgroundDropdownMenuStyles;
