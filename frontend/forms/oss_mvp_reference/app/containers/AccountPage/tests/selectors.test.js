// import { selectAccountPageDomain } from '../selectors';
// import { fromJS } from 'immutable';

// const selector = selectAccountPageDomain();

describe('selectAccountPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toBe(true);
  });
});
