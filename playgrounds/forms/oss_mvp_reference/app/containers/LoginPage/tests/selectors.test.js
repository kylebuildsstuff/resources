// import { selectLoginPageDomain } from '../selectors';
// import { fromJS } from 'immutable';

// const selector = selectLoginPageDomain();

describe('selectorsLoginPage', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toBe(true);
  });
});
